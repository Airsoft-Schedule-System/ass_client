# Project guidelines

## 프로젝트 원칙

- 현재 요구사항에 필요한 코드와 구조만 추가
- 아직 사용하지 않는 계층·폴더·추상화를 미리 생성하지 않기
- 두 곳 이상에서 실제로 사용하는 코드만 공통 코드로 분리
- 기존 구조와 디자인을 먼저 확인하고 임의로 대체하거나 확장하지 않기
- 규칙과 구현이 달라지는 경우 코드와 이 문서를 함께 수정

## 주석

- 주석을 작성할 수 있는 코드 파일 상단에 파일이 담당하는 일을 한 줄로 설명
- 주요 타입·함수·복잡한 분기에는 처음 보는 개발자가 책임과 의도를 이해할 수 있는 주석 작성
- 컴포넌트가 직접 정의한 각 props 선언 옆에 전달값의 역할을 설명하는 인라인 주석 작성
- React와 HTML에서 상속받은 기본 props는 개별 주석 대상에서 제외
- 주석은 `지원할 최소 화면 너비를 지정`처럼 종결어미 없이 짧게 작성
- 코드만으로 명확한 동작을 반복해서 설명하는 주석은 생략
- JSON, 자동 생성 파일, 타입 선언 파일처럼 주석이 불가능하거나 불필요한 파일은 파일 상단 주석 규칙에서 제외

## 파일 및 도메인 구조

- API 코드는 `src/api/<도메인>/`에 배치
- 도메인의 API 진입점은 `<도메인>.ts`, 오류 타입과 변환 로직은 `<도메인>.error.ts`로 분리
- 기능 전용 컴포넌트와 로직은 해당 기능 내부에 배치
- 여러 기능에서 실제로 공유하는 컴포넌트와 타입만 공통 디렉터리에 배치
- 기능별 오류를 하나의 거대한 공통 오류 파일에 누적하지 않기
- `@/` 경로 별칭을 사용하고 깊은 상대 경로 import를 만들지 않기

## React

- 컴포넌트는 화면 표현과 사용자 상호작용에 집중
- 렌더링 중 API 호출, 상태 변경, 구독 등록과 같은 부수효과를 실행하지 않기
- props와 state를 직접 변경하지 않기
- Hook은 컴포넌트 또는 커스텀 Hook의 최상위에서만 호출
- 이벤트와 인증 상태 구독은 등록한 위치에서 정리 함수를 통해 해제
- 상태는 사용하는 가장 가까운 컴포넌트에 두고 불필요한 전역 상태를 만들지 않기
- 한 컴포넌트가 여러 책임을 가질 때만 의미 있는 단위로 분리

## TypeScript

- `any`는 예외 없이 사용하지 않기
- API 입력·응답과 환경변수처럼 외부 데이터와 맞닿는 경계에는 타입을 명시
- 구현 내부에서 타입이 명확한 값은 TypeScript 타입 추론 사용
- 타입만 import할 때는 `import type` 사용
- 사용하지 않는 변수·매개변수·import를 남기지 않기
- 타입 오류를 assertion이나 검사 설정 완화로 우회하지 않기

## API와 Supabase

- React 컴포넌트에서 Supabase 클라이언트를 직접 호출하지 않기
- Supabase 호출은 `src/api/<도메인>/`의 도메인 함수에서 수행
- Supabase 클라이언트 생성과 공통 인증 설정은 `src/lib/supabase/client.ts`에서만 관리
- Supabase 원본 오류는 도메인 오류 변환 함수를 거쳐 화면에 전달
- 세션 조회와 사용자 검증처럼 의미가 다른 작업을 하나의 함수로 합치지 않기
- 인증 상태 구독을 사용한 호출부는 반환된 subscription을 반드시 해제

## 환경변수와 보안

- 로컬 실행에 필요한 값은 Git에서 제외된 `.env`에 작성
- 저장소에는 실제 값 없이 변수 이름만 포함한 `.env.example`만 유지
- 배포 환경의 값은 CI/CD 또는 호스팅 환경에서 주입
- `VITE_` 접두사가 붙은 값은 브라우저에 공개된다는 전제로 사용
- Supabase publishable key만 클라이언트에서 사용
- Supabase secret key, service role key, 비밀번호와 같은 비밀값을 클라이언트 코드나 저장소에 추가하지 않기
- 새 환경변수를 추가하거나 제거할 때 `.env.example`과 `src/vite-env.d.ts`를 함께 수정
- 필수 환경변수는 앱 시작 시 누락 여부를 검증

## 스타일링과 색상

- Tailwind CSS 유틸리티 클래스를 우선 사용
- 글자 크기와 줄높이는 Tailwind 기본 typography 단계인 `text-xs`, `text-sm`, `text-base` 등을 사용
- `text-[13px]`, `leading-[18px]`처럼 임의의 typography 값을 사용하지 않기
- 앱 최상위 레이아웃은 전체 화면 크기와 공통 배경만 관리
- 앱 최상위 레이아웃은 뷰포트 높이를 고정하고 문서 전체의 스크롤을 만들지 않기
- 페이지 공통 콘텐츠 너비와 상하좌우 여백은 해당 페이지 레이아웃에서 관리
- 모바일 레이아웃의 상하 여백은 `safe-area-inset-top`과 `safe-area-inset-bottom`을 기본으로 사용
- 디자인에 필요한 추가 상하 여백은 모바일 레이아웃이 아니라 각 페이지에서 관리
- 스크롤이 필요한 화면은 페이지 내부의 명시적인 스크롤 영역에서만 허용
- 페이지는 헤더·폼·액션 같은 섹션의 배치와 간격을 관리
- 부모 요소는 자식 사이의 간격을 `gap`으로 관리
- 공통 컴포넌트는 자신의 내부 `padding`만 관리
- 자식 컴포넌트에 외부 배치를 위한 `margin`을 지정하지 않기
- 페이지마다 다른 여백을 앱 최상위 레이아웃에 추가하지 않기
- 전체 너비가 필요한 요소는 페이지 레이아웃의 padding 밖에 배치
- 색상은 `src/index.css`에 정의된 기존 CSS 변수 팔레트를 사용
- 실제로 사용하는 색상만 팔레트에 유지하고 같은 색상값은 기존 변수를 재사용
- 컴포넌트에 색상 코드를 직접 작성하지 않기
- 기존 디자인에 없는 색상을 임의로 추가하거나 기존 색상값을 변경하지 않기
- 새 색상이 필요한 경우 디자인 근거와 사용 목적을 확인한 뒤 팔레트에 추가
- 팔레트의 각 색상 변수에는 사용 목적을 인라인 주석으로 작성
- 현재는 모바일 화면을 우선 지원하되 PC 반응형 확장을 막는 구조를 만들지 않기
- 기존 화면 너비 정책을 요구사항 확인 없이 변경하지 않기
- typography 외 임의값은 기존 디자인 토큰이나 Tailwind 기본값으로 표현할 수 없을 때만 사용

## 접근성

- 클릭 동작에는 목적에 맞는 `button` 또는 `a` 요소 사용
- 입력 요소에는 연결된 label 또는 접근 가능한 이름 제공
- 키보드만으로 주요 인증 흐름을 사용할 수 있도록 구현
- 포커스 표시를 제거하지 않고 상태를 색상만으로 전달하지 않기
- 장식용 이미지와 의미 있는 이미지의 대체 텍스트를 구분

## 검증

- 작업 완료 전에 `npm run check`를 실행
- lint, format, TypeScript 또는 build 오류가 있는 상태로 작업을 완료하지 않기
- 자동 수정 명령으로 사용자 코드의 의도가 달라지지 않았는지 diff 확인
- 테스트 도구가 도입되기 전까지 존재하지 않는 테스트 구조나 설정을 임의로 추가하지 않기
- 테스트 도구가 도입되면 로그인·회원가입·로그아웃 같은 핵심 인증 흐름부터 검증

## Git과 커밋

- 하나의 커밋에는 하나의 목적에 해당하는 변경만 포함
- 기존 사용자 변경사항과 현재 작업의 변경사항을 구분
- 관련 없는 파일을 수정하거나 커밋하지 않기
- 커밋 메시지는 프로젝트의 Conventional Commits 형식을 사용
- 허용된 커밋 타입은 `feat`, `fix`, `refactor`, `perf`, `style`, `test`, `docs`, `build`, `ci`, `chore`, `revert`
