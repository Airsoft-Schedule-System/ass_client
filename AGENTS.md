# Project guidelines

## 주석

- 주석은 종결어미 없이 짧게 작성: `지원할 최소 화면 너비를 지정`
- 모든 코드 파일 상단에 파일이 담당하는 일을 한 줄로 설명
- 처음 보는 개발자가 핵심 흐름과 의도를 이해할 수 있도록 주요 타입·함수·분기마다 간결한 주석 작성
- 코드만으로 명확한 동작을 그대로 반복하는 불필요한 주석은 생략

## 스타일링

- Tailwind CSS 유틸리티 클래스 우선
- 현재는 모바일만 지원, 하지만 PC 반응형 스타일 추가할 것을 염두할 것

## 파일 구조

- 오류 타입과 변환 로직은 `auth.error.ts`처럼 개별 도메인·기능 단위로 분리
- 여러 기능에서 실제로 공유하는 최소 오류 타입만 공통 디렉터리에 배치
- 기능별 오류를 하나의 거대한 공통 파일에 누적하지 않기
